---
layout: null
---
<?xml version="1.0" encoding="UTF-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>{{ site.title }} - Todos los Contenidos</title>
  <link href="{{ '/feed.xml' | absolute_url }}" rel="self" type="application/atom+xml"/>
  <link href="{{ '/' | absolute_url }}" rel="alternate" type="text/html"/>
  <updated>{{ site.time | date_to_xmlschema }}</updated>
  <id>{{ '/' | absolute_url }}</id>
  <author>
    <name>{{ site.author }}</name>
  </author>
  <subtitle>{{ site.description }}</subtitle>

  {% comment %}
  Combinar posts y releases en un solo array y ordenar por fecha (desc).
  {% endcomment %}
  {% assign all_items = site.posts | concat: site.releases | sort: 'date' | reverse %}

  {% for item in all_items limit:20 %}
  <entry>
    <title>{{ item.title | xml_escape }}</title>
    <link href="{{ item.url | absolute_url }}" rel="alternate" type="text/html"/>
    <published>{{ item.date | date_to_xmlschema }}</published>
    <updated>{{ item.date | date_to_xmlschema }}</updated>
    <id>{{ item.id | absolute_url }}</id>
    <content type="html" xml:base="{{ item.url | absolute_url }}">
      {{ item.content | xml_escape }}
    </content>
    {% if item.collection == 'releases' %}
    <category term="release"/>
    {% else %}
    <category term="post"/>
    {% endif %}
  </entry>
  {% endfor %}
</feed>
